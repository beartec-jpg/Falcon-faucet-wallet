{
  "buildCommand": "corepack enable && corepack prepare pnpm@9 --activate && pnpm install --frozen-lockfile && pnpm run build",
  "framework": "nextjs",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
